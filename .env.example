# Base URL the frontend calls for the backend API. Must be reachable from
# the browser (this is a NEXT_PUBLIC_ var, so it's baked into the client
# bundle at build time, not read at runtime).
NEXT_PUBLIC_API_URL=http://localhost:8000/api
